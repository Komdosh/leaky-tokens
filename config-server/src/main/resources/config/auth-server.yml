info:
  app:
    name: auth-server

auth:
  jwt:
    issuer: leaky-tokens-auth
    expiry-seconds: 3600
  api-key:
    cache-ttl-seconds: 300
    blacklist-ttl-seconds: 3600

springdoc:
  swagger-ui:
    tags-sorter: alpha
    operations-sorter: alpha
  paths-to-exclude:
    - /actuator/**
